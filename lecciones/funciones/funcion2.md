La función obtener_paginas:

* Esta función no recibe parámetros.
* Su tipo de valor de retorno es INT, lo que indica que devolverá un número entero.
* Dentro del bloque de código de la función, se utiliza la expresión (ROUND(RAND() * 100) * 4) para generar un número aleatorio entre 0 y 100, redondearlo y luego multiplicarlo por 4.
* La función asigna el resultado a la variable @paginas y luego lo retorna.